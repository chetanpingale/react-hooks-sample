# react-hooks-sample

## Available Scripts

In the project directory, you can run:

To run Client
### `npm install`
### `npm start`

To Run Server
### `cd server`
### `npm install`
### `node index.js`